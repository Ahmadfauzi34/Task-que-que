{
  "name": "@task-que-que/workflow-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/worker.ts",
    "test": "bun test",
    "check": "bun test && bun build src/worker.ts --target=bun --outdir=.check-dist"
  }
}
