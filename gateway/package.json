{
  "name": "@task-que-que/bun-gateway",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "test": "bun test",
    "check": "bun test && bun build src/server.ts --target=bun --outdir=.check-dist"
  }
}
